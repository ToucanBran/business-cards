import { BusinessCardFilterPipe } from './business-card-filter.pipe';

describe('BusinessCardFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new BusinessCardFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
